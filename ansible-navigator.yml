---
ansible-navigator:
  color:
    enable: true

  editor:
    command: vim

  execution-environment:
    image: ghcr.io/ansible/creator-ee:v0.20.0
    pull:
      policy: missing

  mode: stdout

  playbook-artifact:
    enable: false
