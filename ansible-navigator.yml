---
ansible-navigator:
  color:
    enable: true

  editor:
    command: vim

  execution-environment:
    image: ghcr.io/ansible/community-ansible-dev-tools:v24.10.0
    pull:
      policy: missing

#  mode: stdout

  playbook-artifact:
    enable: false
